require 'spec_helper'

describe Ea::Validation do
  it 'has a version number' do
    expect(Ea::Validation::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
